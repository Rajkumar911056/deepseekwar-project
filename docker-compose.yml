version: '3.8'

services:
  app:
    image: rajkumar8897/mynewrepo:javaapplication
    ports:
      - "8080:8080"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: overlay
